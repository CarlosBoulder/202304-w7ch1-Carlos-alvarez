sonar.projectKey=LuisCaballe_202304-w6chwe-luis-caballe-carlos-alvarez-back
sonar.organization=luiscaballe

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.test.inclusions=src/**/*.test.ts, src/**/*.test.tsx
sonar.coverage.exclusions=src/**/types.tsx, src/**/*.d.ts, src/loadEnvironment.ts, src/server/index.ts, src/index.ts, src/server/routers/robtosRouter.ts

sonar.javascript.lcov.reportPaths=coverage/lcov.info
